# Integration Tests

Integration tests verify multiple components working together (e.g., API + services).

Run:

```bash
pytest -m integration
```

If markers are not configured:

```bash
pytest tests/
```


