# Endpoints Index

This page is a quick index of endpoints used by the UI.

## System

- `GET /status`

## Trading

- `GET /positions`
- `GET /orders`
- `GET /capital/overview`

## Intelligence

- `GET /opportunities`
- `GET /strategies/performance`
- `GET /market/state`
- `GET /data/health`

For full details, see `REST API`.


