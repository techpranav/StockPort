# Installation

## 1) Install Python dependencies

From repo root:

```bash
pip install -r requirements.txt
```

## 2) (Optional) Install Redis

Follow your platform’s Redis installation. For Windows, you may use Docker or a Redis-compatible service.

## 3) Verify

Run a quick import check:

```bash
python -c "import streamlit; import pandas; import plotly"
```


