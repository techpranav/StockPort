# Market State

Market state is the “context layer” used to interpret signals.

Common fields:

- regime (bullish/bearish/neutral)
- volatility state (low/normal/high)
- breadth state
- liquidity state

See: `APPLICATION_OVERVIEW.md` and any market-state modules under `backend/` / `core/`.


